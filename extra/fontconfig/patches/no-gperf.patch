diff --git a/fc-genericfamily/meson.build b/fc-genericfamily/meson.build
index 4435879..440c17d 100644
--- a/fc-genericfamily/meson.build
+++ b/fc-genericfamily/meson.build
@@ -24,9 +24,3 @@ fcgenericfamily_gperf = custom_target(
     '@OUTPUT@',
   ],
 )
-fcgenericfamily_h = custom_target(
-  'fcgenericfamily.h',
-  input: fcgenericfamily_gperf,
-  output: 'fcgenericfamily.h',
-  command: [gperf, '--pic', '-m', '100', '@INPUT@', '--output-file', '@OUTPUT@'],
-)
diff --git a/meson.build b/meson.build
index 48ae521..94fd770 100644
--- a/meson.build
+++ b/meson.build
@@ -479,8 +479,6 @@ if gperf.found() and get_option('wrap_mode') != 'forcefallback'
     error('unable to determine gperf len type')
   endif
 else
-  # Fallback to subproject
-  gperf = find_program('gperf')
   # assume if we are compiling from the wrap, the size is just size_t
   gperf_len_type = 'size_t'
 endif
diff --git a/src/meson.build b/src/meson.build
index 54fcc32..2d981a4 100644
--- a/src/meson.build
+++ b/src/meson.build
@@ -59,20 +59,12 @@ fcobjshash_gperf = custom_target(
   build_by_default: true,
 )
 
-fcobjshash_h = custom_target(
-  'fcobjshash.h',
-  input: fcobjshash_gperf,
-  output: 'fcobjshash.h',
-  command: [gperf, '--pic', '-m', '100', '@INPUT@', '--output-file', '@OUTPUT@'],
-)
-
 lib_fontconfig_sources = [
   fc_sources,
   alias_headers,
   ft_alias_headers,
   fccase_h,
   fcconst_h,
-  fcgenericfamily_h,
   fclang_h,
   fcobjshash_h,
   fcstdint_h,
